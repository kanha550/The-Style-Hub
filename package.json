{
  "name": "aura-dress-selector-root",
  "private": true,
  "scripts": {
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "node backend/server.js"
  }
}
